[project]
name = "fluence-data"
version = "0.0.0"
description = "Synthetic dialogue generation and corpus preparation (SPEC §5.D)"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.9,<0.10"]
build-backend = "uv_build"
