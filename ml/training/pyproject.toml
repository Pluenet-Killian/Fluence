[project]
name = "fluence-training"
version = "0.0.0"
description = "Task-LoRA distillation pipelines (SPEC §5.D)"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.9,<0.10"]
build-backend = "uv_build"
