{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: the main window loads the hub's local PWA.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
