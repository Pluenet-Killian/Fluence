# apps/cli — fluencectl

**À construire en Phase 2** (PLAN §2, tâche 2.6) : CLI de debug, bench et replay
(`fluencectl health`, `pair`, `watch` d'abord ; `suggest`, `record-gaze` ensuite).

C'est un client de l'API du hub comme les autres (D-2.1) : il consomme
`fluence-protocol` et l'API réseau locale, jamais d'accès privilégié.

Licence : AGPL-3.0-only (application, D-10.1).
