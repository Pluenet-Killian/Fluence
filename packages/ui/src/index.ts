// SPDX-License-Identifier: Apache-2.0

/**
 * `@fluence/ui` — shared accessibility-first components (SPEC §2.B, §7).
 *
 * Will host the building blocks every Fluence client reuses: dwell-aware
 * targets that register themselves with the input engine (§4.A), the
 * three fixed suggestion slots with anti-flicker rules (§7.A), high-contrast
 * AA+ themes where no information is carried by color alone.
 *
 * Components consume the same input protocol as everything else
 * (dogfooding rule, §7.A): the composer is built from these blocks.
 *
 * First components land with the web composer (PLAN Phase 5). Until then
 * this package exports nothing.
 *
 * @packageDocumentation
 */

export {};
